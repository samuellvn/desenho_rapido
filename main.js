var randomNumber=randomNumberf();

function randomNumberf=Math.random(a, b) {
    return Math.floor(Math.random()*(a - b + 1)) + a
}

console.log(randomNumber);


function name(){}
    
quickDrawDataset();
array1=["pen","paper","book","bottle"];
randomNo = Math.floor((Math.random() *array1.length)+1);
Element_of_array = array1[randomNo];